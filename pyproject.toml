[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-trading-engine-v2"
version = "0.1.0"
description = "HERMES Trading Engine v2 scaffold"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["hermes*"]
